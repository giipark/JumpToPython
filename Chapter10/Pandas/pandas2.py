import pandas as pd

df = pd.read_csv('test.csv')
print(df)
print("=" * 50)
# ================================================================================================
print(df.loc[125, ['Name', 'Age', 'Ticket']])
print("=" * 50)
# ================================================================================================
print(df.info())
print("=" * 50)
# ================================================================================================
print(df.describe())  # int64, float64의 type의 feature들만 통계치를 계산
print("=" * 50)
# ================================================================================================
print("Age 평균:", df['Age'].mean(), "/ Age 표준편차:", df['Age'].std())
print("=" * 50)
# ================================================================================================
df1 = df[['Age', 'Name']]
df2 = df[['PassengerId', 'Pclass']]

df = pd.concat([df1, df2])  # pd.concat은 행ㄷ 단위로 이어붙였기 때문에 NaN이 다수 포함되어 있음
print(df)
print("=" * 50)
# ================================================================================================
df = pd.concat([df1, df2], axis=1)
print(df)